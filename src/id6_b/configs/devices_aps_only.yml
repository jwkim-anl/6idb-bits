# Guarneri-style device YAML configuration

# Objects only available at APS

apstools.devices.ApsMachineParametersDevice:
- name: aps
  labels:
  - aps_machine
  # "baseline" removed: ApsCycleComputedRO.get() raises UnboundLocalError when
  # APS cycle data is unavailable (apstools bug). Restore when apstools is fixed.
