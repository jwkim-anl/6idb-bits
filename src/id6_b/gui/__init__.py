"""Qt desktop interface for the 6-ID-B Bluesky session.

This package is an *additive* front end: it starts a Jupyter kernel that runs
the same ``from id6_b.startup import *`` bootstrap as the plain IPython
session, so the existing command-line workflow is unaffected.

Launch with the ``id6b-gui`` console script.
"""
